# v0.5 — i18n Implementation Plan (Sprint 1 of 2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Git policy:** Commits at end of each task are written as suggested commands. Do NOT execute commits without the maintainer's explicit confirmation per project policy. The plan documents *what* to commit; the maintainer decides *when*.

**Goal:** Добавить интернационализацию (RU + EN с auto-detect системной локали) в claudesync. main process становится i18n-blind: все строки в renderer передаются как `LocalizedMessage = { key, params?, fallback? }`, renderer переводит. Все 17 существующих компонентов и пользовательские строки в main мигрируются.

**Architecture:**
Lightweight кастомный i18n (~80–120 строк). `LocaleProvider` оборачивает App, `useT()` даёт `t(key, params)`. Словари — два JSON-файла EN/RU с плоскими dotted-keys; для плюрализации значение становится объектом с CLDR-категориями (`one`/`few`/`many`/`other`) через `Intl.PluralRules`. Локаль хранится в `AppConfig.locale: 'en' | 'ru' | null` (null = follow `app.getLocale()`).

**Tech Stack:** Electron + React 18 + TypeScript, Vitest для тестов, без новых runtime-deps.

**Reference spec:** [docs/superpowers/specs/2026-05-08-v0.5-i18n-and-conflicts.md](../specs/2026-05-08-v0.5-i18n-and-conflicts.md)

---

## File Map

**New:**
- `src/renderer/i18n/index.tsx` — pure `translate()`, `LocaleProvider`, `useT()`, `useLocale()`
- `src/renderer/i18n/pluralize.ts` — wrapper над `Intl.PluralRules`
- `src/renderer/i18n/locales/en.json` — английский словарь
- `src/renderer/i18n/locales/ru.json` — русский словарь
- `tests/renderer/i18n.test.ts` — тесты для `translate()` (pure function)
- `tests/renderer/pluralize.test.ts` — тесты `pluralize()`
- `tests/renderer/dictionaries.test.ts` — parity check EN↔RU

**Modify:**
- `src/shared/api.ts` — добавить `LocalizedMessage`, `locale` в AppConfig, `getSystemLocale()` в AppApi, обновить `RunResult.error`/`PushStepEvent.message`/`InitStepEvent.message`
- `src/main/config.ts` — парсить и писать `locale`
- `src/main/ipc.ts` — `getSystemLocale` handler + миграция `error`-строк
- `src/main/push.ts` — миграция строк на `LocalizedMessage`
- `src/main/init-wizard.ts` — миграция строк
- `src/preload/index.ts` — экспортировать `getSystemLocale`
- `src/renderer/main.tsx` — обернуть `<App/>` в `<LocaleProvider>`
- `src/renderer/App.tsx` — заменить inline-строки на `useT()`
- `src/renderer/components/*.tsx` — все 17 компонентов — `useT()`
- `src/renderer/components/steps/*.tsx` — все 4 step-компонента
- `tests/main/config.test.ts` — добавить кейсы для `locale`
- `tests/main/ipc.test.ts` — добавить кейс на `getSystemLocale`

---

## Task Sequence

Task 1–9 — foundation (pure helpers + provider + Settings UI).
Task 10 — types для main↔renderer моста.
Task 11–13 — миграция main process.
Task 14–20 — миграция renderer компонентов (по группам).
Task 21 — финальная верификация.

---

### Task 1: Add `locale` field to `AppConfig`

**Files:**
- Modify: `src/shared/api.ts:5-10` (`AppConfig` type)
- Modify: `src/main/config.ts:30-50` (`readConfig`)
- Test: `tests/main/config.test.ts` (add cases)

- [ ] **Step 1: Add failing tests in `tests/main/config.test.ts`**

В блок `describe('readConfig', () => { ... })` добавить три новых `it`:

```ts
it('reads locale from config file (en)', () => {
  const f = join(dir, 'config.json')
  writeFileSync(f, JSON.stringify({ rulesTarget: '/x', locale: 'en' }))
  expect(readConfig(f).locale).toBe('en')
})

it('reads locale from config file (ru)', () => {
  const f = join(dir, 'config.json')
  writeFileSync(f, JSON.stringify({ rulesTarget: '/x', locale: 'ru' }))
  expect(readConfig(f).locale).toBe('ru')
})

it('returns null locale when missing', () => {
  const f = join(dir, 'config.json')
  writeFileSync(f, JSON.stringify({ rulesTarget: '/x' }))
  expect(readConfig(f).locale).toBeNull()
})

it('returns null locale for unsupported value', () => {
  const f = join(dir, 'config.json')
  writeFileSync(f, JSON.stringify({ rulesTarget: '/x', locale: 'fr' }))
  expect(readConfig(f).locale).toBeNull()
})
```

Также апдейт существующих `expect(...).toEqual({ repoPath: ..., repoUrl: ..., rulesTarget: ..., includeSecretsInPush: ... })` — добавить `locale: null` в каждое ожидание (5 мест в файле).

- [ ] **Step 2: Run tests, verify failure**

```bash
npx vitest run tests/main/config.test.ts
```
Expected: FAIL — `Property 'locale' does not exist on type 'AppConfig'`.

- [ ] **Step 3: Extend `AppConfig` type in `src/shared/api.ts`**

Заменить блок `export type AppConfig = { ... }`:

```ts
export type AppConfig = {
  repoPath: string | null
  repoUrl: string | null
  rulesTarget: string | null
  includeSecretsInPush: boolean
  locale: 'en' | 'ru' | null
}
```

- [ ] **Step 4: Update `readConfig` and fallback in `src/main/config.ts`**

Заменить функцию `readConfig`:

```ts
export function readConfig(filePath: string): AppConfig {
  const fallback: AppConfig = {
    repoPath: null,
    repoUrl: null,
    rulesTarget: null,
    includeSecretsInPush: false,
    locale: null,
  }
  if (!existsSync(filePath)) return fallback
  try {
    const raw = readFileSync(filePath, 'utf8')
    const parsed = JSON.parse(raw) as Partial<AppConfig>
    const locale = parsed.locale === 'en' || parsed.locale === 'ru' ? parsed.locale : null
    return {
      repoPath: typeof parsed.repoPath === 'string' ? parsed.repoPath : null,
      repoUrl: typeof parsed.repoUrl === 'string' ? parsed.repoUrl : null,
      rulesTarget: typeof parsed.rulesTarget === 'string' ? parsed.rulesTarget : null,
      includeSecretsInPush: parsed.includeSecretsInPush === true,
      locale,
    }
  } catch {
    return fallback
  }
}
```

- [ ] **Step 5: Fix Settings.tsx call-site that constructs AppConfig**

`src/renderer/components/Settings.tsx:75-80` — в вызове `setConfig({...})` добавить `locale: null` (поведение не меняется, поле просто прокидывается; в Task 18 будет полноценная UI):

```ts
const r = await window.api.setConfig({
  repoUrl: trimmedUrl || null,
  repoPath: finalPath,
  rulesTarget: trimmedTarget || null,
  includeSecretsInPush: false,
  locale: null,
})
```

- [ ] **Step 6: Run tests + typecheck**

```bash
npx vitest run tests/main/config.test.ts
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: all PASS.

- [ ] **Step 7: Commit**

```bash
git add src/shared/api.ts src/main/config.ts src/renderer/components/Settings.tsx tests/main/config.test.ts
git commit -m "feat(i18n): add locale field to AppConfig"
```

---

### Task 2: `getSystemLocale` IPC

**Files:**
- Modify: `src/shared/api.ts` (`AppApi`)
- Modify: `src/main/ipc.ts` (handler)
- Modify: `src/preload/index.ts` (expose)
- Test: `tests/main/ipc.test.ts` (add case)

- [ ] **Step 1: Add failing test in `tests/main/ipc.test.ts`**

(Открыть файл, посмотреть существующий стиль mock'инга `ipcMain.handle`. Добавить `it` блок в подходящий describe, либо создать новый.)

```ts
it('registers get-system-locale handler that returns app.getLocale()', async () => {
  // mock electron's app.getLocale before registerIpc — see existing tests
  mockApp.getLocale.mockReturnValue('ru-RU')
  registerIpc(deps)
  const handler = handlers.get('get-system-locale')
  expect(handler).toBeTruthy()
  const result = await handler!()
  expect(result).toBe('ru-RU')
})
```

(Адаптировать к существующему shape mock'ов в `tests/main/ipc.test.ts`.)

- [ ] **Step 2: Run test, verify failure**

```bash
npx vitest run tests/main/ipc.test.ts
```
Expected: FAIL — handler не зарегистрирован.

- [ ] **Step 3: Add `getSystemLocale` to `AppApi` in `src/shared/api.ts`**

В блок `interface AppApi { ... }` (рядом с `getPlatform`) добавить:

```ts
  getSystemLocale(): Promise<string>
```

- [ ] **Step 4: Register handler in `src/main/ipc.ts`**

Найти существующий блок с `ipcMain.handle('get-platform', ...)` и рядом добавить:

```ts
ipcMain.handle('get-system-locale', () => app.getLocale())
```

`app` уже импортирован из 'electron' (строка 3).

- [ ] **Step 5: Expose in `src/preload/index.ts`**

В объекте `api` рядом с `getPlatform`:

```ts
  getSystemLocale: (): Promise<string> => ipcRenderer.invoke('get-system-locale'),
```

- [ ] **Step 6: Run test + typecheck**

```bash
npx vitest run tests/main/ipc.test.ts
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/shared/api.ts src/main/ipc.ts src/preload/index.ts tests/main/ipc.test.ts
git commit -m "feat(i18n): add getSystemLocale IPC"
```

---

### Task 3: `pluralize.ts`

**Files:**
- Create: `src/renderer/i18n/pluralize.ts`
- Test: `tests/renderer/pluralize.test.ts`

- [ ] **Step 1: Write failing tests in `tests/renderer/pluralize.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { pluralCategory } from '../../src/renderer/i18n/pluralize'

describe('pluralCategory', () => {
  it('EN — 1 → one, others → other', () => {
    expect(pluralCategory('en', 1)).toBe('one')
    expect(pluralCategory('en', 0)).toBe('other')
    expect(pluralCategory('en', 2)).toBe('other')
    expect(pluralCategory('en', 100)).toBe('other')
  })

  it('RU — covers one/few/many/other', () => {
    expect(pluralCategory('ru', 1)).toBe('one')
    expect(pluralCategory('ru', 21)).toBe('one')
    expect(pluralCategory('ru', 2)).toBe('few')
    expect(pluralCategory('ru', 22)).toBe('few')
    expect(pluralCategory('ru', 5)).toBe('many')
    expect(pluralCategory('ru', 11)).toBe('many')
    expect(pluralCategory('ru', 25)).toBe('many')
    // 'other' для не-целых дробных, например:
    expect(pluralCategory('ru', 1.5)).toBe('other')
  })

  it('coerces non-finite to 0/other', () => {
    expect(pluralCategory('en', NaN)).toBe('other')
    expect(pluralCategory('ru', Infinity)).toBe('other')
  })
})
```

- [ ] **Step 2: Run, verify failure**

```bash
npx vitest run tests/renderer/pluralize.test.ts
```
Expected: FAIL (file does not exist).

- [ ] **Step 3: Implement `src/renderer/i18n/pluralize.ts`**

```ts
export type PluralCategory = 'zero' | 'one' | 'two' | 'few' | 'many' | 'other'

const cache = new Map<string, Intl.PluralRules>()

function getRules(locale: string): Intl.PluralRules {
  let r = cache.get(locale)
  if (!r) {
    r = new Intl.PluralRules(locale)
    cache.set(locale, r)
  }
  return r
}

export function pluralCategory(locale: string, count: number): PluralCategory {
  if (!Number.isFinite(count)) return 'other'
  return getRules(locale).select(count) as PluralCategory
}
```

- [ ] **Step 4: Run, verify pass**

```bash
npx vitest run tests/renderer/pluralize.test.ts
```
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/renderer/i18n/pluralize.ts tests/renderer/pluralize.test.ts
git commit -m "feat(i18n): add pluralCategory helper"
```

---

### Task 4: Pure `translate()` function + dictionaries

**Files:**
- Create: `src/renderer/i18n/index.tsx` (только pure-helpers, no React yet)
- Create: `src/renderer/i18n/locales/en.json` (seed)
- Create: `src/renderer/i18n/locales/ru.json` (seed)
- Test: `tests/renderer/i18n.test.ts`

- [ ] **Step 1: Create seed dictionaries**

`src/renderer/i18n/locales/en.json`:
```json
{
  "common.cancel": "Cancel",
  "common.save": "Save",
  "test.greeting": "Hello, {{name}}",
  "test.files": {
    "one": "{{count}} file",
    "other": "{{count}} files"
  }
}
```

`src/renderer/i18n/locales/ru.json`:
```json
{
  "common.cancel": "Отмена",
  "common.save": "Сохранить",
  "test.greeting": "Привет, {{name}}",
  "test.files": {
    "one": "{{count}} файл",
    "few": "{{count}} файла",
    "many": "{{count}} файлов",
    "other": "{{count}} файла"
  }
}
```

(Эти ключи нужны, чтобы тесты нашли реальные данные. Они так и останутся — `common.cancel`/`common.save` пригодятся в дальнейших миграциях; `test.*` удалим в Task 21.)

- [ ] **Step 2: Write failing tests in `tests/renderer/i18n.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { translate } from '../../src/renderer/i18n'
import en from '../../src/renderer/i18n/locales/en.json'
import ru from '../../src/renderer/i18n/locales/ru.json'

const dicts = { en, ru }

describe('translate', () => {
  it('returns string for existing key', () => {
    expect(translate('en', dicts, 'common.cancel')).toBe('Cancel')
    expect(translate('ru', dicts, 'common.cancel')).toBe('Отмена')
  })

  it('interpolates {{var}} placeholders', () => {
    expect(translate('en', dicts, 'test.greeting', { name: 'Dan' })).toBe('Hello, Dan')
    expect(translate('ru', dicts, 'test.greeting', { name: 'Дан' })).toBe('Привет, Дан')
  })

  it('falls back to en when key missing in current locale', () => {
    const partial = { en: { 'only.in.en': 'EN value' }, ru: {} }
    expect(translate('ru', partial, 'only.in.en')).toBe('EN value')
  })

  it('returns key as last resort when missing in both', () => {
    expect(translate('en', dicts, 'totally.missing.key')).toBe('totally.missing.key')
  })

  it('selects plural category by count', () => {
    expect(translate('en', dicts, 'test.files', { count: 1 })).toBe('1 file')
    expect(translate('en', dicts, 'test.files', { count: 5 })).toBe('5 files')
    expect(translate('ru', dicts, 'test.files', { count: 1 })).toBe('1 файл')
    expect(translate('ru', dicts, 'test.files', { count: 2 })).toBe('2 файла')
    expect(translate('ru', dicts, 'test.files', { count: 5 })).toBe('5 файлов')
  })

  it('plural falls back to other when category missing', () => {
    const partial = { en: { 'p': { other: 'X' } as Record<string, string> }, ru: {} }
    expect(translate('en', partial, 'p', { count: 1 })).toBe('X')
  })

  it('missing param renders as empty string', () => {
    expect(translate('en', dicts, 'test.greeting')).toBe('Hello, ')
  })
})
```

- [ ] **Step 3: Run, verify failure**

```bash
npx vitest run tests/renderer/i18n.test.ts
```
Expected: FAIL (`translate` not exported).

- [ ] **Step 4: Implement pure helpers in `src/renderer/i18n/index.tsx`**

```tsx
import { pluralCategory } from './pluralize'

export type Locale = 'en' | 'ru'
export const SUPPORTED: Locale[] = ['en', 'ru']

export type DictValue = string | { [category: string]: string }
export type Dict = Record<string, DictValue>
export type Dicts = Record<Locale, Dict>

export type TParams = Record<string, string | number>

function lookup(d: Dict, key: string): DictValue | undefined {
  return d[key]
}

function interpolate(template: string, params: TParams): string {
  return template.replace(/\{\{(\w+)\}\}/g, (_, k: string) => {
    const v = params[k]
    return v == null ? '' : String(v)
  })
}

export function translate(
  locale: Locale,
  dicts: Dicts,
  key: string,
  params?: TParams,
): string {
  let raw: DictValue | undefined = lookup(dicts[locale] ?? {}, key)
  if (raw == null && locale !== 'en') raw = lookup(dicts.en ?? {}, key)
  if (raw == null) {
    if (typeof process !== 'undefined' && process.env?.NODE_ENV !== 'production') {
      // eslint-disable-next-line no-console
      console.warn(`i18n miss: ${key}`)
    }
    return key
  }

  let str: string
  if (typeof raw === 'object') {
    const count = Number(params?.count ?? 0)
    const cat = pluralCategory(locale, count)
    str =
      raw[cat] ??
      raw.other ??
      raw[Object.keys(raw)[0] ?? ''] ??
      key
  } else {
    str = raw
  }
  return interpolate(str, params ?? {})
}

export function normalizeSystemLocale(raw: string | undefined): Locale {
  if (!raw) return 'en'
  if (raw.toLowerCase().startsWith('ru')) return 'ru'
  return 'en'
}
```

- [ ] **Step 5: Verify TypeScript can import JSON**

Если возникает ошибка типа `Cannot find module './locales/en.json'` — добавить в `tsconfig.json` (renderer's tsconfig) `"resolveJsonModule": true` (если ещё нет).

```bash
npx tsc --noEmit -p tsconfig.json
```

Если есть ошибки про JSON — открыть `tsconfig.json`, в `compilerOptions` добавить:
```json
"resolveJsonModule": true,
"esModuleInterop": true
```

(`esModuleInterop` для default-импорта JSON.)

- [ ] **Step 6: Run tests, verify pass**

```bash
npx vitest run tests/renderer/i18n.test.ts
```
Expected: PASS (все 7 кейсов).

- [ ] **Step 7: Commit**

```bash
git add src/renderer/i18n/index.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json tests/renderer/i18n.test.ts tsconfig.json
git commit -m "feat(i18n): translate() with interpolation + plural"
```

---

### Task 5: Dictionaries parity test

**Files:**
- Test: `tests/renderer/dictionaries.test.ts`

- [ ] **Step 1: Write parity test**

```ts
import { describe, it, expect } from 'vitest'
import en from '../../src/renderer/i18n/locales/en.json'
import ru from '../../src/renderer/i18n/locales/ru.json'

const PLURAL_CATEGORIES_RU = ['one', 'few', 'many', 'other'] as const
const PLURAL_CATEGORIES_EN = ['one', 'other'] as const

function placeholders(s: string): string[] {
  const m = s.match(/\{\{(\w+)\}\}/g) ?? []
  return [...new Set(m)].sort()
}

function collectPlaceholders(value: unknown): string[] {
  if (typeof value === 'string') return placeholders(value)
  if (value && typeof value === 'object') {
    const set = new Set<string>()
    for (const v of Object.values(value as Record<string, unknown>)) {
      if (typeof v === 'string') for (const p of placeholders(v)) set.add(p)
    }
    return [...set].sort()
  }
  return []
}

describe('dictionaries parity', () => {
  it('every EN key exists in RU', () => {
    const missing = Object.keys(en).filter((k) => !(k in ru))
    expect(missing).toEqual([])
  })

  it('every RU key exists in EN (no orphan RU keys)', () => {
    const missing = Object.keys(ru).filter((k) => !(k in en))
    expect(missing).toEqual([])
  })

  it('plural keys: EN has one+other, RU has one+few+many+other', () => {
    for (const [k, v] of Object.entries(en)) {
      if (typeof v !== 'object') continue
      for (const cat of PLURAL_CATEGORIES_EN) {
        expect(v, `EN[${k}] must have category ${cat}`).toHaveProperty(cat)
      }
      const ruVal = (ru as Record<string, unknown>)[k]
      expect(typeof ruVal, `RU[${k}] must also be a plural object`).toBe('object')
      for (const cat of PLURAL_CATEGORIES_RU) {
        expect(ruVal, `RU[${k}] must have category ${cat}`).toHaveProperty(cat)
      }
    }
  })

  it('placeholders match between EN and RU for each key', () => {
    for (const k of Object.keys(en)) {
      const enP = collectPlaceholders((en as Record<string, unknown>)[k])
      const ruP = collectPlaceholders((ru as Record<string, unknown>)[k])
      expect(ruP, `placeholders mismatch in key "${k}"`).toEqual(enP)
    }
  })
})
```

- [ ] **Step 2: Run, verify pass against seed dictionaries**

```bash
npx vitest run tests/renderer/dictionaries.test.ts
```
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/renderer/dictionaries.test.ts
git commit -m "test(i18n): dictionaries parity check"
```

---

### Task 6: `LocaleProvider`, `useT`, `useLocale`

**Files:**
- Modify: `src/renderer/i18n/index.tsx` (append React-bound API)

- [ ] **Step 1: Append React API to `src/renderer/i18n/index.tsx`**

В конец файла добавить:

```tsx
import React, { createContext, useContext, useEffect, useMemo, useState } from 'react'
import enDict from './locales/en.json'
import ruDict from './locales/ru.json'

const dicts: Dicts = { en: enDict as Dict, ru: ruDict as Dict }

type LocaleContextValue = {
  locale: Locale
  preference: Locale | null     // null = follow system
  setPreference: (p: Locale | null) => Promise<void>
  t: (key: string, params?: TParams) => string
}

const LocaleContext = createContext<LocaleContextValue | null>(null)

export function LocaleProvider({ children }: { children: React.ReactNode }): JSX.Element {
  const [preference, setPreferenceState] = useState<Locale | null>(null)
  const [systemLocale, setSystemLocale] = useState<Locale>('en')
  const [ready, setReady] = useState(false)

  useEffect(() => {
    let cancelled = false
    void (async () => {
      const cfg = await window.api.getConfig()
      const sys = await window.api.getSystemLocale()
      if (cancelled) return
      setSystemLocale(normalizeSystemLocale(sys))
      setPreferenceState(cfg.locale)
      setReady(true)
    })()
    return () => {
      cancelled = true
    }
  }, [])

  const locale: Locale = preference ?? systemLocale

  const value = useMemo<LocaleContextValue>(
    () => ({
      locale,
      preference,
      setPreference: async (p) => {
        const cfg = await window.api.getConfig()
        const r = await window.api.setConfig({ ...cfg, locale: p })
        if (!r.ok) throw new Error(r.error ?? 'setConfig failed')
        setPreferenceState(p)
      },
      t: (key, params) => translate(locale, dicts, key, params),
    }),
    [locale, preference],
  )

  if (!ready) return <></>
  return <LocaleContext.Provider value={value}>{children}</LocaleContext.Provider>
}

export function useT(): (key: string, params?: TParams) => string {
  const ctx = useContext(LocaleContext)
  if (!ctx) throw new Error('useT must be used within <LocaleProvider>')
  return ctx.t
}

export function useLocale(): {
  locale: Locale
  preference: Locale | null
  setPreference: (p: Locale | null) => Promise<void>
} {
  const ctx = useContext(LocaleContext)
  if (!ctx) throw new Error('useLocale must be used within <LocaleProvider>')
  const { locale, preference, setPreference } = ctx
  return { locale, preference, setPreference }
}
```

(`SetConfigResult.error` сейчас `string`. После Task 10 он станет `LocalizedMessage`. На этом этапе оставляем `r.error ?? 'setConfig failed'` как есть; в Task 10 сделаем правку.)

- [ ] **Step 2: Typecheck**

```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: PASS.

- [ ] **Step 3: Existing tests still pass**

```bash
npx vitest run
```
Expected: all PASS.

- [ ] **Step 4: Commit**

```bash
git add src/renderer/i18n/index.tsx
git commit -m "feat(i18n): LocaleProvider + useT + useLocale"
```

---

### Task 7: Wire `LocaleProvider` in `main.tsx`

**Files:**
- Modify: `src/renderer/main.tsx`

- [ ] **Step 1: Wrap `<App/>`**

Заменить файл `src/renderer/main.tsx` целиком:

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { App } from './App'
import { LocaleProvider } from './i18n'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LocaleProvider>
      <App />
    </LocaleProvider>
  </React.StrictMode>,
)
```

- [ ] **Step 2: Run dev server, verify app loads**

```bash
npm run dev
```
Expected: app окно открывается, текст в нём (всё ещё английский — миграции компонентов нет). Никаких ошибок в DevTools console про `useT`/`useLocale` (никто пока не использует).

Закрыть окно.

- [ ] **Step 3: Typecheck + lint**

```bash
npx tsc --noEmit -p tsconfig.json
npm run lint
```
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/renderer/main.tsx
git commit -m "feat(i18n): wrap App in LocaleProvider"
```

---

### Task 8: `t()` smoke — first useT consumer

Добавим использование `useT` в один маленький компонент (Tabs), чтобы убедиться, что цепочка работает end-to-end до миграции всего UI.

**Files:**
- Modify: `src/renderer/components/Tabs.tsx`
- Modify: `src/renderer/i18n/locales/en.json`, `ru.json`

- [ ] **Step 1: Прочитать текущий `Tabs.tsx`**

```bash
cat src/renderer/components/Tabs.tsx
```

(или Read tool)

Сейчас компонент рендерит лейблы `tabs[].label` напрямую. App.tsx передаёт `[{ id: 'sync', label: 'Sync' }, { id: 'plugins', label: 'Plugins' }]` хардкодом.

Стратегия: Tabs остаётся generic — он просто рендерит `label`. Перевод делает родитель в App.tsx. Чтобы протестировать `useT`, мигрируем строку в App.tsx прямо сейчас (только tabs labels).

- [ ] **Step 2: Add tab keys to dictionaries**

`en.json` — добавить:
```json
  "tabs.sync": "Sync",
  "tabs.plugins": "Plugins"
```

`ru.json` — добавить:
```json
  "tabs.sync": "Синхронизация",
  "tabs.plugins": "Плагины"
```

(Помнить: `dictionaries.test.ts` теперь требует наличия в обоих языках.)

- [ ] **Step 3: Use `useT` in `App.tsx`**

В `src/renderer/App.tsx` в начало функции `App()` добавить:
```tsx
const t = useT()
```

Импорт сверху:
```tsx
import { useT } from './i18n'
```

Заменить:
```tsx
tabs={[
  { id: 'sync', label: 'Sync' },
  { id: 'plugins', label: 'Plugins' },
]}
```
на:
```tsx
tabs={[
  { id: 'sync', label: t('tabs.sync') },
  { id: 'plugins', label: t('tabs.plugins') },
]}
```

- [ ] **Step 4: Manual smoke**

```bash
npm run dev
```
Открыть app. Tabs должны показать «Sync» / «Plugins».

(Опционально проверить RU: открыть DevTools, в renderer console: `await window.api.setConfig({ ...(await window.api.getConfig()), locale: 'ru' })`, перезагрузить app. Tabs должны стать «Синхронизация» / «Плагины». После — вернуть `locale: null`.)

- [ ] **Step 5: Tests + typecheck + lint**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
```
Expected: all PASS.

- [ ] **Step 6: Commit**

```bash
git add src/renderer/App.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate tab labels (first useT consumer)"
```

---

### Task 9: Settings — Language radio UI

**Files:**
- Modify: `src/renderer/components/Settings.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Add language keys to dictionaries**

`en.json`:
```json
  "settings.language.title": "Language",
  "settings.language.system": "System",
  "settings.language.en": "English",
  "settings.language.ru": "Русский"
```

`ru.json`:
```json
  "settings.language.title": "Язык",
  "settings.language.system": "Системный",
  "settings.language.en": "English",
  "settings.language.ru": "Русский"
```

(Названия языков на английском оставляем как есть в RU — convention для переключателей языка: на родном языке отображается own endonym.)

- [ ] **Step 2: Add Language section in Settings.tsx**

В `src/renderer/components/Settings.tsx`:

(а) Импорт сверху:
```tsx
import { useT, useLocale, SUPPORTED } from '../i18n'
```

(б) В начале `Settings()` функции:
```tsx
const t = useT()
const { preference, setPreference } = useLocale()
```

(в) Перед `<div className="mt-2 flex justify-end gap-2">` (последний блок с Cancel/Save) вставить новую секцию:

```tsx
<div className="my-4 border-t border-neutral-200 pt-4 dark:border-neutral-700">
  <h3 className="mb-2 text-xs uppercase text-neutral-500">{t('settings.language.title')}</h3>
  <div className="flex flex-col gap-1 text-sm">
    <label className="flex items-center gap-2">
      <input
        type="radio"
        name="locale"
        checked={preference === null}
        onChange={() => void setPreference(null)}
      />
      {t('settings.language.system')}
    </label>
    {SUPPORTED.map((loc) => (
      <label key={loc} className="flex items-center gap-2">
        <input
          type="radio"
          name="locale"
          checked={preference === loc}
          onChange={() => void setPreference(loc)}
        />
        {t(`settings.language.${loc}`)}
      </label>
    ))}
  </div>
</div>
```

(г) В существующем `setConfig({ ... locale: null })` (Task 1) заменить `locale: null` на `locale: preference` — чтобы Save не ронял пользовательский выбор:

```tsx
locale: preference,
```

- [ ] **Step 3: Manual smoke**

```bash
npm run dev
```
- Открыть Settings. Внизу должна быть секция «Language» с тремя радио.
- По-умолчанию выбран «System».
- Кликнуть «Русский» → весь UI, который уже мигрирован (Tabs), переключается мгновенно.
- Кликнуть «System» → вернулся системный язык.
- Перезапустить app — выбор сохранился.

- [ ] **Step 4: Tests + typecheck + lint**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
```
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/Settings.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): Language radio in Settings"
```

---

### Task 10: `LocalizedMessage` type + payload migration in `shared/api.ts`

**Files:**
- Modify: `src/shared/api.ts`
- Modify: `src/renderer/i18n/index.tsx` (helper `tMessage`)

- [ ] **Step 1: Add `LocalizedMessage` type**

В `src/shared/api.ts` сразу после `export type RunResult`:

```ts
export type LocalizedMessage = {
  key: string
  params?: Record<string, string | number>
  /** Optional english fallback used when key missing OR for raw external text (git stderr tail) */
  fallback?: string
}
```

- [ ] **Step 2: Migrate `RunResult`, `SetConfigResult`, `PushStepEvent`, `InitStepEvent`, `DeviceFlowResult`, `StepEvent` to use `LocalizedMessage`**

Заменить:
```ts
export type RunResult = { ok: boolean; exitCode: number; error?: string }
```
на:
```ts
export type RunResult = { ok: boolean; exitCode: number; error?: LocalizedMessage }
```

Заменить:
```ts
export type SetConfigResult = { ok: boolean; error?: string }
```
на:
```ts
export type SetConfigResult = { ok: boolean; error?: LocalizedMessage }
```

Заменить:
```ts
export type StepEvent = { step: StepName; status: StepStatus; message?: string }
```
на:
```ts
export type StepEvent = { step: StepName; status: StepStatus; message?: LocalizedMessage }
```

Аналогично — `PushStepEvent`, `InitStepEvent`. И `DeviceFlowResult`:

```ts
export type DeviceFlowResult =
  | { ok: true }
  | { ok: false; error: LocalizedMessage }
```

`applyPluginChanges` IPC возвращает `{ ok: boolean; error?: string }` — поменять `error?: string` на `error?: LocalizedMessage` в `AppApi`:

```ts
applyPluginChanges(changes: ApplyPluginChanges): Promise<{ ok: boolean; error?: LocalizedMessage }>
```

- [ ] **Step 3: Add `tMessage` helper в `src/renderer/i18n/index.tsx`**

В конец файла:

```tsx
import type { LocalizedMessage } from '@shared/api'

export function tMessage(
  t: (key: string, params?: TParams) => string,
  m: LocalizedMessage | undefined,
): string {
  if (!m) return ''
  const translated = t(m.key, m.params)
  return translated === m.key && m.fallback ? m.fallback : translated
}
```

- [ ] **Step 4: Compile errors expected**

```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: множество ошибок типа «Type 'string' is not assignable to type 'LocalizedMessage | undefined'» в `push.ts`, `init-wizard.ts`, `ipc.ts`, в renderer-компонентах. Это нормально — мы их разрулим в Tasks 11–20.

Чтобы временно не блокировать развитие, в этом коммите мы НЕ чиним call-sites в main — они переведутся сразу в Task 11. Чтобы коммит компилировался, переходим сразу к Task 11 и делаем Tasks 10+11 одним коммитом.

- [ ] **Step 5: НЕ коммитим отдельно — переходим к Task 11**

Task 10 и 11 объединяются в один коммит.

---

### Task 11: Migrate `push.ts` strings to `LocalizedMessage`

**Files:**
- Modify: `src/main/push.ts`

- [ ] **Step 1: Add error keys to dictionaries**

`en.json`:
```json
  "push.error.notConfigured": "Sync not configured. Open Settings to set Repo URL.",
  "push.error.notSignedIn": "Not signed in to GitHub.",
  "push.error.invalidJson": "Invalid JSON in global/settings.json — fix it before push.",
  "push.error.gitStatusFailed": "git status failed.",
  "push.error.commitFailed": "Commit failed.",
  "push.error.pushExit": "Push failed (exit {{code}}).",
  "push.error.network": "Network error talking to GitHub. Check VPN/proxy and click Push to retry.{{tail}}",
  "push.error.auth": "GitHub auth rejected. Sign out and sign in again, then retry.{{tail}}",
  "push.error.conflict": "Merge conflict during rebase. Resolve manually in {{repoPath}}, then click Push again.",
  "push.error.pullOther": "git pull --rebase failed{{tail}}. See log above.",
  "push.info.nothingToPush": "Nothing to push — local config matches repo.",
  "push.info.networkRetry": "Network glitch — retrying pull in 2s…"
```

`ru.json`:
```json
  "push.error.notConfigured": "Синхронизация не настроена. Откройте «Настройки» и укажите URL репозитория.",
  "push.error.notSignedIn": "Вы не вошли в GitHub.",
  "push.error.invalidJson": "Невалидный JSON в global/settings.json — исправьте перед push.",
  "push.error.gitStatusFailed": "git status завершился с ошибкой.",
  "push.error.commitFailed": "Не удалось создать коммит.",
  "push.error.pushExit": "Push завершился ошибкой (код {{code}}).",
  "push.error.network": "Сетевая ошибка при обращении к GitHub. Проверьте VPN/прокси и повторите push.{{tail}}",
  "push.error.auth": "GitHub отклонил авторизацию. Выйдите и войдите снова, затем повторите push.{{tail}}",
  "push.error.conflict": "Конфликт при rebase. Разрешите вручную в {{repoPath}} и нажмите Push ещё раз.",
  "push.error.pullOther": "git pull --rebase завершился ошибкой{{tail}}. Подробности — в логе выше.",
  "push.info.nothingToPush": "Нечего отправлять — локальная конфигурация совпадает с репозиторием.",
  "push.info.networkRetry": "Сетевая ошибка — повторная попытка pull через 2 секунды…"
```

- [ ] **Step 2: Migrate `push.ts`**

Заменить `failResult` helper:

```ts
function failResult(error: LocalizedMessage): RunResult {
  return { ok: false, exitCode: -1, error }
}
```

(Импорт `LocalizedMessage` из `@shared/api`.)

Заменить `pullErrorMessage`:

```ts
function pullErrorLocalized(kind: PullErrorKind, repoPath: string, stderr: string): LocalizedMessage {
  const tail = stderr.trim().split(/\r?\n/).filter(Boolean).slice(-2).join(' | ')
  const tailParam = tail ? ` — ${tail}` : ''
  switch (kind) {
    case 'network':
      return { key: 'push.error.network', params: { tail: tailParam } }
    case 'auth':
      return { key: 'push.error.auth', params: { tail: tailParam } }
    case 'conflict':
      return { key: 'push.error.conflict', params: { repoPath } }
    default:
      return { key: 'push.error.pullOther', params: { tail: tail ? `: ${tail}` : '' } }
  }
}
```

(Старую `pullErrorMessage` удалить.)

Все call-sites `failResult('...')` заменить на `failResult({ key: '...' })`. Полный список изменений в `runPush`:

```ts
if (!cfg.repoUrl || !cfg.repoPath || !cfg.rulesTarget) {
  return failResult({ key: 'push.error.notConfigured' })
}
const token = loadToken(opts.userDataDir)
if (!token) return failResult({ key: 'push.error.notSignedIn' })
```

```ts
} catch (e) {
  opts.emitStep({ step: 'export', status: 'failed', message: { key: 'push.error.invalidJson', fallback: (e as Error).message } })
  return failResult({ key: 'push.error.invalidJson', fallback: (e as Error).message })
}
```

(Логика такая: для `(e as Error).message` от внутреннего `JSON.parse` — реальная diagnostic уйдёт в `fallback` (на английском, для отладки), а пользовательское сообщение `t('push.error.invalidJson')` показывается renderer'ом.)

```ts
if (status.exitCode !== 0) return failResult({ key: 'push.error.gitStatusFailed' })
if (status.stdout.trim() === '') {
  return { ok: true, exitCode: 0, error: { key: 'push.info.nothingToPush' } }
}
```

```ts
opts.emit({
  time: new Date().toTimeString().slice(0, 8),
  text: 'Network glitch — retrying pull in 2s…',
  level: 'info',
})
```

`emit` (LogLine) — оставляем raw string, потому что лог не переводится (см. spec). Но если хотим отображать «Network glitch» по-русски при показе — это уйдёт через emitStep, а лог остаётся английским. Альтернативно можно сделать `emit({ text: t('push.info.networkRetry') })` если LogLine.text станет LocalizedMessage. Spec явно говорит лог не переводим — оставляем как есть.

```ts
opts.emitStep({ step: 'pull', status: 'failed', message: pullErrorLocalized(kind2, repoPath, rebase.stderr) })
return failResult(pullErrorLocalized(kind2, repoPath, rebase.stderr))
```

(И аналогично для одиночного fail.)

```ts
if (commit.exitCode !== 0) {
  opts.emitStep({ step: 'commit', status: 'failed' })
  return failResult({ key: 'push.error.commitFailed' })
}
```

```ts
if (push.exitCode !== 0) {
  opts.emitStep({ step: 'push', status: 'failed' })
  return failResult({ key: 'push.error.pushExit', params: { code: push.exitCode } })
}
```

И в catch:
```ts
}).catch((e: Error) => failResult({ key: 'push.error.pullOther', fallback: e.message }))
```

- [ ] **Step 3: Update `tests/main/push.test.ts`**

Существующие assertion'ы на `error` — теперь `error.key`. Например:
```ts
expect(result.error).toEqual({ key: 'push.error.notSignedIn' })
```

(Конкретные правки зависят от существующих кейсов; engineer проходится по файлу и адаптирует строковые ожидания.)

- [ ] **Step 4: Verify**

```bash
npx vitest run tests/main/push.test.ts
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: PASS (push.test) и PASS (typecheck для main/shared). renderer typecheck может ещё ругаться на старые call-sites — починим в Tasks 12–20.

- [ ] **Step 5: Combined commit (Task 10 + Task 11)**

```bash
git add src/shared/api.ts src/renderer/i18n/index.tsx src/main/push.ts src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json tests/main/push.test.ts
git commit -m "feat(i18n): LocalizedMessage type + migrate push.ts strings"
```

---

### Task 12: Migrate `init-wizard.ts` strings

**Files:**
- Modify: `src/main/init-wizard.ts`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Read current strings**

```bash
grep -n "error\|return.*fail\|message:" src/main/init-wizard.ts | head -50
```

(Engineer выписывает все строковые сообщения, формирует список ключей.)

- [ ] **Step 2: Add keys**

`en.json`:
```json
  "init.error.notSignedIn": "Not signed in to GitHub.",
  "init.error.createRepoFailed": "Failed to create repo: {{reason}}",
  "init.error.cloneFailed": "git clone failed (exit {{code}}).",
  "init.error.commitFailed": "Initial commit failed.",
  "init.error.pushFailed": "Push to new repo failed (exit {{code}}).",
  "init.error.scanFailed": "Could not scan local config: {{reason}}",
  "init.step.createRepo": "Creating repo on GitHub",
  "init.step.clone": "Cloning empty repo",
  "init.step.generate": "Generating global/ structure",
  "init.step.commit": "Initial commit",
  "init.step.push": "Pushing"
```

`ru.json`:
```json
  "init.error.notSignedIn": "Вы не вошли в GitHub.",
  "init.error.createRepoFailed": "Не удалось создать репозиторий: {{reason}}",
  "init.error.cloneFailed": "git clone завершился ошибкой (код {{code}}).",
  "init.error.commitFailed": "Не удалось создать первый коммит.",
  "init.error.pushFailed": "Push в новый репозиторий завершился ошибкой (код {{code}}).",
  "init.error.scanFailed": "Не удалось просканировать локальную конфигурацию: {{reason}}",
  "init.step.createRepo": "Создаю репозиторий на GitHub",
  "init.step.clone": "Клонирую пустой репозиторий",
  "init.step.generate": "Генерирую структуру global/",
  "init.step.commit": "Первый коммит",
  "init.step.push": "Push"
```

- [ ] **Step 3: Migrate `init-wizard.ts`**

Каждое `return { ok: false, exitCode: ..., error: 'string' }` → `error: { key: '...', params?: { ... } }`.
Каждое `emitStep({ ..., message: 'string' })` → `message: { key: '...' }`.
Сохранять сырые `(e as Error).message` в `fallback`.

(Конкретный diff зависит от текущего содержимого `init-wizard.ts`. Engineer проходит файл, заменяет каждое упоминание.)

- [ ] **Step 4: Update `tests/main/init-wizard.test.ts`** — assertions на `.error.key`.

- [ ] **Step 5: Verify**

```bash
npx vitest run tests/main/init-wizard.test.ts
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/main/init-wizard.ts src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json tests/main/init-wizard.test.ts
git commit -m "feat(i18n): migrate init-wizard.ts strings"
```

---

### Task 13: Migrate `ipc.ts` error strings

**Files:**
- Modify: `src/main/ipc.ts`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Identify error strings**

Поиск:
```bash
grep -n "error:\|reject\|return.*ok: false" src/main/ipc.ts
```

В основном это validation в `setConfig` (validateLocalRepo / validateRepoUrl / validateRulesTarget — они возвращают `{ ok: false, error: string }`). Эти строки сейчас в `config.ts`:

- `'Local repo path is required'`
- `'Local repo path must be absolute'`
- `'Cannot stat: ...'`
- `'Local repo path must be a directory'`
- `"Folder is not empty and not a git repo — pick a fresh path or an existing clone"`
- `'Repo URL is required'`
- `"Invalid URL format (expected https:// or git@)"`
- `'Rules target folder is required'`
- `'Rules target folder must be absolute'`

И `apply-plugin-changes` handler возвращает error-строки.

- [ ] **Step 2: Add keys to dictionaries**

`en.json`:
```json
  "config.error.localRepoRequired": "Local repo path is required.",
  "config.error.localRepoAbsolute": "Local repo path must be absolute.",
  "config.error.localRepoStat": "Cannot stat: {{reason}}",
  "config.error.localRepoNotDir": "Local repo path must be a directory.",
  "config.error.localRepoNotEmpty": "Folder is not empty and not a git repo — pick a fresh path or an existing clone.",
  "config.error.urlRequired": "Repo URL is required.",
  "config.error.urlInvalid": "Invalid URL format (expected https:// or git@).",
  "config.error.targetRequired": "Rules target folder is required.",
  "config.error.targetAbsolute": "Rules target folder must be absolute.",
  "plugins.error.applyFailed": "Failed to apply plugin changes: {{reason}}"
```

`ru.json`:
```json
  "config.error.localRepoRequired": "Укажите путь к локальному репозиторию.",
  "config.error.localRepoAbsolute": "Путь к локальному репозиторию должен быть абсолютным.",
  "config.error.localRepoStat": "Не удалось получить информацию о пути: {{reason}}",
  "config.error.localRepoNotDir": "Путь должен указывать на папку.",
  "config.error.localRepoNotEmpty": "Папка не пуста и не является git-репозиторием — выберите пустую папку или существующий клон.",
  "config.error.urlRequired": "Укажите URL репозитория.",
  "config.error.urlInvalid": "Некорректный формат URL (ожидается https:// или git@).",
  "config.error.targetRequired": "Укажите папку с правилами.",
  "config.error.targetAbsolute": "Путь к папке с правилами должен быть абсолютным.",
  "plugins.error.applyFailed": "Не удалось применить изменения плагинов: {{reason}}"
```

- [ ] **Step 3: Decision — `config.ts` validators**

`config.ts` validators возвращают `{ ok: false; error: string }` сейчас. Чтобы не ломать backward compat и позволить call-sites решать: оставить `string` в `ValidationResult`, но при использовании в `setConfig` IPC обернуть в `LocalizedMessage` через словарь string→key. Альтернатива — мигрировать `ValidationResult`.

Решение: мигрировать `ValidationResult` тоже. Заменить:

```ts
export type ValidationResult = { ok: true } | { ok: false; error: LocalizedMessage }
```

И в каждом `validate*` возвращать `error: { key: '...' }` или с params.

(Импорт `LocalizedMessage` в `config.ts` из `@shared/api`.)

- [ ] **Step 4: Migrate `validateLocalRepo`, `validateRepoUrl`, `validateRulesTarget`**

`validateLocalRepo` пример:
```ts
export function validateLocalRepo(p: string): ValidationResult {
  if (!p) return { ok: false, error: { key: 'config.error.localRepoRequired' } }
  const expanded = expandTilde(p)
  if (!isAbsolute(expanded)) return { ok: false, error: { key: 'config.error.localRepoAbsolute' } }
  if (!existsSync(expanded)) return { ok: true }
  let st
  try { st = statSync(expanded) } catch (e) {
    return { ok: false, error: { key: 'config.error.localRepoStat', params: { reason: (e as Error).message } } }
  }
  if (!st.isDirectory()) return { ok: false, error: { key: 'config.error.localRepoNotDir' } }
  const entries = readdirSync(expanded)
  if (entries.length === 0) return { ok: true }
  if (entries.includes('.git')) return { ok: true }
  return { ok: false, error: { key: 'config.error.localRepoNotEmpty' } }
}
```

Аналогично для `validateRepoUrl` и `validateRulesTarget`.

- [ ] **Step 5: Update `tests/main/config.test.ts`**

Существующие `expect(r.error).toMatch(/required/i)` — уже не подходят. Заменить на:
```ts
if (!r.ok) expect(r.error.key).toBe('config.error.localRepoRequired')
```

И аналогично остальные. (Конкретные правки — engineer проходит по файлу.)

- [ ] **Step 6: Migrate `ipc.ts` call-sites**

Где `validate*` возвращают `{ ok: false, error: string }` и эта строка используется в `setConfig` ответе — теперь `error` — `LocalizedMessage`, прокидываем напрямую:

```ts
const v1 = validateRulesTarget(c.rulesTarget ?? '')
if (!v1.ok) return { ok: false, error: v1.error }
```

(Поскольку `SetConfigResult.error` теперь `LocalizedMessage`, типы сходятся.)

`apply-plugin-changes` handler — error wrap'ить в `{ key: 'plugins.error.applyFailed', params: { reason: e.message } }`.

- [ ] **Step 7: Update `tests/main/ipc.test.ts`**

Assertions на error.key.

- [ ] **Step 8: Verify**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: main + shared typecheck PASS. Renderer typecheck может ругаться — починим в 14–20.

- [ ] **Step 9: Commit**

```bash
git add src/main/ipc.ts src/main/config.ts src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json tests/main/ipc.test.ts tests/main/config.test.ts
git commit -m "feat(i18n): migrate ipc.ts and config validators to LocalizedMessage"
```

---

### Task 14: Migrate Header + small leaf components

**Files:**
- Modify: `src/renderer/components/Header.tsx`
- Modify: `src/renderer/components/PullButton.tsx`
- Modify: `src/renderer/components/PushButton.tsx`
- Modify: `src/renderer/components/StepList.tsx`
- Modify: `src/renderer/components/LogConsole.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Add keys**

(Engineer открывает каждый компонент, выписывает все user-visible текстовые литералы, придумывает иерархические ключи. Пример для Header: `header.signIn`, `header.signOut`, `header.openSettings`, `header.repoNotSet` и т.д. Аналогично остальные.)

Add ~30–40 keys to `en.json` и параллельно в `ru.json` (зеркально).

Пример (в реальности набор будет шире — engineer формирует под фактический текст):

`en.json`:
```json
  "header.openSettings": "Settings",
  "header.signedInAs": "@{{login}}",
  "step.fetch": "Fetch",
  "step.install": "Install",
  "step.export": "Export local rules",
  "step.pull": "Pull from remote",
  "step.commit": "Commit",
  "step.push": "Push",
  "step.status.idle": "Idle",
  "step.status.running": "Running…",
  "step.status.done": "Done",
  "step.status.failed": "Failed",
  "log.clear": "Clear",
  "log.empty": "(log empty)",
  "pull.button": "Pull & Install",
  "push.button": "Push Local Changes"
```

`ru.json` — зеркально с переводом.

- [ ] **Step 2: Migrate each component**

В каждом из 5 компонентов:
1. Добавить `import { useT, tMessage } from '../i18n'` (tMessage только если компонент рендерит `LocalizedMessage`-сообщение).
2. В начале функции: `const t = useT()`.
3. Заменить каждый user-facing string-литерал на `t('key')` или `tMessage(t, message)` для динамических `LocalizedMessage`.

Примеры:

`Header.tsx` — кнопка settings:
```tsx
<button onClick={onOpenSettings} title={t('header.openSettings')}>⚙</button>
```

`StepList.tsx` — лейблы шагов и статусов:
```tsx
function stepLabel(t: TFn, step: StepName): string {
  return t(`step.${step}`)
}
function statusLabel(t: TFn, status: StepStatus): string {
  return t(`step.status.${status}`)
}
// и при отрисовке message:
{step.message && <span>{tMessage(t, step.message)}</span>}
```

`PullButton.tsx`, `PushButton.tsx` — текст кнопок: `t('pull.button')`, `t('push.button')`.

`LogConsole.tsx` — `t('log.clear')`, `t('log.empty')`.

- [ ] **Step 3: Verify**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
```

Запустить app:
```bash
npm run dev
```
Просмотреть UI — все 5 компонентов отображают корректные строки в EN и RU (через Settings → Language).

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/Header.tsx src/renderer/components/PullButton.tsx src/renderer/components/PushButton.tsx src/renderer/components/StepList.tsx src/renderer/components/LogConsole.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate Header + step + log + buttons"
```

---

### Task 15: Migrate `PushModal` + `EnvPromptModal` + `DeviceFlowModal`

**Files:**
- Modify: `src/renderer/components/PushModal.tsx`
- Modify: `src/renderer/components/EnvPromptModal.tsx`
- Modify: `src/renderer/components/DeviceFlowModal.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Identify strings и Add keys**

Для каждого компонента — заголовки, лейблы полей, кнопки, hint-текст, error-сообщения. Эстимат: ~30 ключей.

Пример блока в `en.json`:
```json
  "push.modal.title": "Push local changes",
  "push.modal.commitLabel": "Commit message",
  "push.modal.commitPlaceholder": "Update from claudesync",
  "push.modal.includeSecrets": "Include API keys (env block) — only for PRIVATE repos",
  "push.modal.confirm": "Push",
  "deviceFlow.title": "Sign in to GitHub",
  "deviceFlow.codeLabel": "Enter this code in your browser:",
  "deviceFlow.openBrowser": "Open browser",
  "deviceFlow.copyCode": "Copy code",
  "deviceFlow.checking": "Checking…",
  "deviceFlow.checkNow": "Check now",
  "deviceFlow.success": "Signed in as @{{login}}",
  "deviceFlow.expired": "Authorization expired. Try again.",
  "envPrompt.title": "{{plugin}} requires API keys",
  "envPrompt.save": "Save",
  "envPrompt.cancel": "Cancel"
```

(И в `ru.json` зеркально.)

- [ ] **Step 2: Migrate components**

Аналогично Task 14 — `useT()`, заменить литералы.

- [ ] **Step 3: Verify + manual smoke**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
npm run dev
```

Открыть Push modal, env prompt, device flow — все на правильном языке.

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/PushModal.tsx src/renderer/components/EnvPromptModal.tsx src/renderer/components/DeviceFlowModal.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate PushModal, EnvPromptModal, DeviceFlowModal"
```

---

### Task 16: Migrate `InitWizard` + `steps/*`

**Files:**
- Modify: `src/renderer/components/InitWizard.tsx`
- Modify: `src/renderer/components/steps/SignInStep.tsx`
- Modify: `src/renderer/components/steps/RepoSettingsStep.tsx`
- Modify: `src/renderer/components/steps/PreviewStep.tsx`
- Modify: `src/renderer/components/steps/ProgressStep.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

- [ ] **Step 1: Inventory strings**

```bash
grep -nE '"[A-Z][^"]{2,}"|>[A-Z][a-zA-Z ,.!?:;()'\''-]+<' src/renderer/components/InitWizard.tsx src/renderer/components/steps/*.tsx | grep -v className
```

Ожидаемые группы:
- `InitWizard.tsx` — заголовок «Initialize new repo», кнопки навигации (Back/Next/Cancel), индикатор шагов («Step 1 of 4» и т.д.).
- `SignInStep.tsx` — «Sign in to GitHub», описание, кнопка «Sign in with GitHub», статус «Signed in as @login», «Continue».
- `RepoSettingsStep.tsx` — лейблы «Repo name», «Owner», «Visibility (Private / Public)», «Description», hint-тексты, validation errors.
- `PreviewStep.tsx` — заголовки колонок «Files to include» / «Excluded», warning про env-block, «Create & Push».
- `ProgressStep.tsx` — лейблы шагов («Creating repo on GitHub», «Cloning empty repo», …), «Try again», «Open repo», «Close».

- [ ] **Step 2: Add keys**

`en.json`:
```json
  "init.title": "Initialize new repo from current config",
  "init.step.indicator": "Step {{current}} of {{total}}",
  "init.nav.back": "Back",
  "init.nav.next": "Continue",
  "init.nav.cancel": "Cancel",
  "init.signIn.title": "Sign in to GitHub",
  "init.signIn.description": "claudesync needs GitHub access to create repos and push commits on your behalf.",
  "init.signIn.signedIn": "Signed in as @{{login}}",
  "init.repoSettings.name": "Repo name",
  "init.repoSettings.namePlaceholder": "claudesync-config",
  "init.repoSettings.owner": "Owner",
  "init.repoSettings.visibility": "Visibility",
  "init.repoSettings.private": "Private",
  "init.repoSettings.public": "Public",
  "init.repoSettings.description": "Description",
  "init.repoSettings.descriptionPlaceholder": "(optional)",
  "init.repoSettings.error.nameInvalid": "Name must match [a-zA-Z0-9._-]+",
  "init.repoSettings.error.nameRequired": "Name is required",
  "init.preview.includeTitle": "Files to include",
  "init.preview.excludeTitle": "Excluded",
  "init.preview.envWarning": "API keys in settings.json will be stripped — re-add them via Plugin manager after pushing.",
  "init.preview.confirm": "Create & Push",
  "init.progress.openRepo": "Open repo",
  "init.progress.tryAgain": "Try again",
  "init.progress.close": "Close",
  "init.progress.success": "Done!"
```

(Шаги ProgressStep — `init.step.createRepo` и т.д. — уже добавлены в Task 12, переиспользуем.)

`ru.json` — зеркально:
```json
  "init.title": "Создать новый репозиторий из текущей конфигурации",
  "init.step.indicator": "Шаг {{current}} из {{total}}",
  "init.nav.back": "Назад",
  "init.nav.next": "Продолжить",
  "init.nav.cancel": "Отмена",
  "init.signIn.title": "Вход в GitHub",
  "init.signIn.description": "claudesync нужен доступ к GitHub, чтобы создавать репозитории и отправлять коммиты от вашего имени.",
  "init.signIn.signedIn": "Вход выполнен как @{{login}}",
  "init.repoSettings.name": "Имя репозитория",
  "init.repoSettings.namePlaceholder": "claudesync-config",
  "init.repoSettings.owner": "Владелец",
  "init.repoSettings.visibility": "Видимость",
  "init.repoSettings.private": "Приватный",
  "init.repoSettings.public": "Публичный",
  "init.repoSettings.description": "Описание",
  "init.repoSettings.descriptionPlaceholder": "(необязательно)",
  "init.repoSettings.error.nameInvalid": "Имя должно соответствовать [a-zA-Z0-9._-]+",
  "init.repoSettings.error.nameRequired": "Имя обязательно",
  "init.preview.includeTitle": "Файлы для загрузки",
  "init.preview.excludeTitle": "Исключённые",
  "init.preview.envWarning": "API-ключи из settings.json будут вырезаны — добавьте их обратно через менеджер плагинов после push'а.",
  "init.preview.confirm": "Создать и отправить",
  "init.progress.openRepo": "Открыть репозиторий",
  "init.progress.tryAgain": "Повторить",
  "init.progress.close": "Закрыть",
  "init.progress.success": "Готово!"
```

- [ ] **Step 3: Migrate each component**

Применить тот же паттерн, что в Task 14:
1. `import { useT, tMessage } from '../i18n'` (или `'../../i18n'` для steps/).
2. `const t = useT()` в начале компонента.
3. Каждый user-facing литерал → `t('key')` или `t('key', { params })`.
4. Динамические `LocalizedMessage` (например, error от main в ProgressStep) → `tMessage(t, message)`.

`ProgressStep.tsx` — ключевая особенность: рендерит шаги по step-id (`'create-repo'`, `'clone'`, etc.). Лейблы:

```tsx
const STEP_LABELS: Record<InitStep, string> = {
  'create-repo': t('init.step.createRepo'),
  clone: t('init.step.clone'),
  generate: t('init.step.generate'),
  commit: t('init.step.commit'),
  push: t('init.step.push'),
}
```

`RepoSettingsStep.tsx` — validation: текущий regex check `/^[a-zA-Z0-9._-]+$/` для имени; ошибки теперь `t('init.repoSettings.error.nameInvalid')` / `t('init.repoSettings.error.nameRequired')`.

- [ ] **Step 4: Verify + manual smoke**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
npm run dev
```

Открыть InitWizard (потребуется конфиг без `repoUrl`). Пройтись по всем 4 шагам в EN и RU. Проверить визуально, что все строки переведены.

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/InitWizard.tsx src/renderer/components/steps/*.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate InitWizard + step components"
```

---

### Task 17: Migrate `PluginsTab`

**Files:**
- Modify: `src/renderer/components/PluginsTab.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

500 строк, ~70 текстовых литералов. Самый большой компонент.

- [ ] **Step 1: Inventory strings**

```bash
grep -nE '"[A-Z][a-zA-Z ,.!?:;()'\''-]*"|>[A-Z][a-zA-Z ,.!?:;()'\''-]+<' src/renderer/components/PluginsTab.tsx | grep -vE 'className|^.*: *"'
```

Результат прохода: ожидаются группы строк
- header tab: «Plugins», «Presets», поиск placeholder, фильтр-чипы.
- plugin card: «Install», «Installed», «Remove», «Loading…», «Updating…», метки tags, «Requires API key».
- preset card: «Apply preset», «X plugins», описание.
- empty / error states: «No plugins match your search.», «Failed to load catalog.», «Try again».
- env modal triggers: «Set API keys», «Open docs».
- counts: «{{count}} plugins installed» (plural).

- [ ] **Step 2: Add ~70 keys to dictionaries**

Namespace `plugins.*`. Структура (примеры — фактический список engineer формирует во время прохода):

`en.json`:
```json
  "plugins.tab.title": "Plugins",
  "plugins.tab.presets": "Presets",
  "plugins.search.placeholder": "Search plugins…",
  "plugins.empty": "No plugins match your search.",
  "plugins.loading": "Loading catalog…",
  "plugins.error.loadFailed": "Failed to load catalog. {{reason}}",
  "plugins.error.tryAgain": "Try again",
  "plugins.card.install": "Install",
  "plugins.card.installed": "Installed",
  "plugins.card.remove": "Remove",
  "plugins.card.installing": "Installing…",
  "plugins.card.removing": "Removing…",
  "plugins.card.requiresEnv": "Requires API key",
  "plugins.card.setKeys": "Set API keys",
  "plugins.card.openDocs": "Open docs",
  "plugins.preset.apply": "Apply preset",
  "plugins.preset.applying": "Applying…",
  "plugins.preset.count": {
    "one": "{{count}} plugin",
    "other": "{{count}} plugins"
  },
  "plugins.installed.count": {
    "one": "{{count}} plugin installed",
    "other": "{{count}} plugins installed"
  },
  "plugins.tag.recommended": "Recommended"
```

`ru.json` — зеркально с переводом (помнить про `one/few/many/other` в плюральных значениях):
```json
  "plugins.tab.title": "Плагины",
  "plugins.tab.presets": "Пресеты",
  "plugins.search.placeholder": "Поиск плагинов…",
  "plugins.empty": "Нет плагинов по вашему запросу.",
  "plugins.loading": "Загружаю каталог…",
  "plugins.error.loadFailed": "Не удалось загрузить каталог. {{reason}}",
  "plugins.error.tryAgain": "Повторить",
  "plugins.card.install": "Установить",
  "plugins.card.installed": "Установлен",
  "plugins.card.remove": "Удалить",
  "plugins.card.installing": "Устанавливаю…",
  "plugins.card.removing": "Удаляю…",
  "plugins.card.requiresEnv": "Нужен API-ключ",
  "plugins.card.setKeys": "Указать ключи",
  "plugins.card.openDocs": "Открыть документацию",
  "plugins.preset.apply": "Применить пресет",
  "plugins.preset.applying": "Применяю…",
  "plugins.preset.count": {
    "one": "{{count}} плагин",
    "few": "{{count}} плагина",
    "many": "{{count}} плагинов",
    "other": "{{count}} плагина"
  },
  "plugins.installed.count": {
    "one": "{{count}} плагин установлен",
    "few": "{{count}} плагина установлено",
    "many": "{{count}} плагинов установлено",
    "other": "{{count}} плагина установлено"
  },
  "plugins.tag.recommended": "Рекомендуем"
```

- [ ] **Step 3: Migrate component**

`import { useT, tMessage } from '../i18n'` сверху. `const t = useT()` в начале функции.

Замены литералов:
- `"Install"` → `t('plugins.card.install')`
- `"Installed"` → `t('plugins.card.installed')`
- ... и так далее по списку из Step 1.

Форматированные счётчики — заменить шаблонные строки на `t()`:
```tsx
// было:
<span>{preset.pluginIds.length} plugins</span>
// стало:
<span>{t('plugins.preset.count', { count: preset.pluginIds.length })}</span>
```

Динамические сообщения от main (например, `applyPluginChanges` error) — теперь `LocalizedMessage`, рендерим через `tMessage(t, error)`.

- [ ] **Step 4: Verify + manual**

```bash
npx vitest run
npx tsc --noEmit -p tsconfig.json
npm run lint
npm run dev
```

Кликнуть Plugins tab. Проверить:
- В EN — все строки английские.
- Через Settings → Русский — переключение мгновенное, RU corrent.
- Применить preset с 3 плагинами → текст counter «3 плагина» (RU).
- Применить preset с 1 плагином → «1 плагин».
- Применить preset с 5 плагинами → «5 плагинов».
- Триггернуть error (например, удалить network) → ошибка по-русски, `tMessage` сработал.

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/PluginsTab.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate PluginsTab with plural counters"
```

---

### Task 18: Finish migrating `Settings.tsx`

**Files:**
- Modify: `src/renderer/components/Settings.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

В Task 9 мы добавили Language section и `useT`/`useLocale`. Остальные строки в Settings ещё английские. Сейчас мигрируем оставшееся.

- [ ] **Step 1: Inventory**

Все литералы в `Settings.tsx`: `"Settings"` (заголовок), `"Repo URL"`, `"(optional — only for Sync)"`, `"https://github.com/user/ai-rules"` (placeholder), `"Rules target folder"`, `"(required)"`, `"Browse…"`, `"▾ Advanced"` / `"▸ Advanced"`, `"Local repo path"`, `"(where to clone — auto-managed if empty)"`, `"Auto-managed by app"`, `"Path to your AI rules folder (e.g. ~/.claude)"`, `"GitHub"`, `"Signed in as"`, `"Sign out"`, `"Sign in with GitHub"`, `"Cancel"`, `"Save"`, error message `"Unknown error"`, error display.

- [ ] **Step 2: Add keys**

```json
  "settings.title": "Settings",
  "settings.repoUrl.label": "Repo URL",
  "settings.repoUrl.optionalHint": "(optional — only for Sync)",
  "settings.repoUrl.placeholder": "https://github.com/user/ai-rules",
  "settings.target.label": "Rules target folder",
  "settings.target.requiredHint": "(required)",
  "settings.browse": "Browse…",
  "settings.advanced.show": "Advanced",
  "settings.advanced.hide": "Advanced",
  "settings.localRepo.label": "Local repo path",
  "settings.localRepo.hint": "(where to clone — auto-managed if empty)",
  "settings.localRepo.placeholder": "Auto-managed by app",
  "settings.target.placeholder": "Path to your AI rules folder (e.g. ~/.claude)",
  "settings.github.title": "GitHub",
  "settings.github.signedInAs": "Signed in as @{{login}}",
  "settings.github.signOut": "Sign out",
  "settings.github.signIn": "Sign in with GitHub",
  "settings.error.unknown": "Unknown error"
```

(Note: для `settings.advanced.show/hide` — в коде `{showAdvanced ? '▾ Advanced' : '▸ Advanced'}` — стрелочки оставляем литералом, переводим только слово: `t('settings.advanced.toggle')` + стрелочка снаружи.)

`ru.json` — зеркально:
```json
  "settings.title": "Настройки",
  "settings.repoUrl.label": "URL репозитория",
  "settings.repoUrl.optionalHint": "(опционально — только для синхронизации)",
  "settings.target.label": "Папка с правилами",
  "settings.target.requiredHint": "(обязательно)",
  "settings.browse": "Обзор…",
  "settings.advanced.show": "Дополнительно",
  "settings.advanced.hide": "Дополнительно",
  ...
```

- [ ] **Step 3: Replace literals**

Замены прямолинейные. Display error `setError(r.error ?? 'Unknown error')` — теперь `r.error` это `LocalizedMessage`. Изменить на:
```ts
setError(r.error ?? null)
```
И тип state: `useState<LocalizedMessage | null>(null)`. Render:
```tsx
{error && <div className="mb-2 text-sm text-red-500">{tMessage(t, error)}</div>}
```

- [ ] **Step 4: Verify + manual**

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/Settings.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate remaining Settings strings"
```

---

### Task 19: Migrate `App.tsx` inline messages

**Files:**
- Modify: `src/renderer/App.tsx`
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

Сейчас в `App.tsx` остались inline-строки в "no repo" блоке: `"No repo configured."`, `"Initialize new repo from current config"`, `"Or set Repo URL in"`, `"⚙ Settings"`, `"if you already have one."`, `"▾ Hide log"`, `"▸ Show log"`.

- [ ] **Step 1: Add keys**

```json
  "sync.noRepo.title": "No repo configured.",
  "sync.noRepo.initialize": "Initialize new repo from current config",
  "sync.noRepo.orSet": "Or set Repo URL in",
  "sync.noRepo.settings": "⚙ Settings",
  "sync.noRepo.ifHave": "if you already have one.",
  "sync.log.show": "Show log",
  "sync.log.hide": "Hide log"
```

(И RU зеркально.)

- [ ] **Step 2: Replace literals**

Использовать `t()`. Стрелочки `▾`/`▸` оставить вне ключа, прицепить снаружи:
```tsx
{showDetails ? `▾ ${t('sync.log.hide')}` : `▸ ${t('sync.log.show')}`}
```

Та же стратегия для `⚙ ${t('sync.noRepo.settings')}` — но тогда лишнее повторение. Лучше держать иконку внутри ключа, как в EN: `"sync.noRepo.settings": "⚙ Settings"` — иконка не зависит от языка, переводится только «Settings».

Для составной фразы «Or set Repo URL in / ⚙ Settings / if you already have one.» — в JSX это inline + кнопка-link. Оставить три отдельных ключа и сцепку JSX:

```tsx
<div className="text-xs text-neutral-500">
  {t('sync.noRepo.orSet')}{' '}
  <button className="text-blue-500" onClick={openSettings}>
    {t('sync.noRepo.settings')}
  </button>{' '}
  {t('sync.noRepo.ifHave')}
</div>
```

- [ ] **Step 3: Verify + manual smoke**

Cold start без repoUrl — должно показывать «No repo configured» / «Repo не настроен» (RU).

- [ ] **Step 4: Commit**

```bash
git add src/renderer/App.tsx src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "feat(i18n): translate App.tsx inline messages"
```

---

### Task 20: Strip seed test keys + final dictionary review

**Files:**
- Modify: `src/renderer/i18n/locales/{en,ru}.json`

В Task 4 мы засунули в словари `test.greeting` и `test.files` для тестов `i18n.test.ts`. Они больше не используются нигде, кроме теста. Решение: оставить (тесты импортируют живые словари — ключи нужны для тестов). НО: эти ключи захламляют словари, и `dictionaries.test.ts` всё равно проверяет parity.

Альтернатива — вынести test-fixture словари в сам тест-файл. Это чище.

- [ ] **Step 1: Inline test fixtures in `tests/renderer/i18n.test.ts`**

Заменить начало файла:

```ts
import { describe, it, expect } from 'vitest'
import { translate } from '../../src/renderer/i18n'

const en = {
  'common.cancel': 'Cancel',
  'common.save': 'Save',
  'test.greeting': 'Hello, {{name}}',
  'test.files': { one: '{{count}} file', other: '{{count}} files' },
} as const

const ru = {
  'common.cancel': 'Отмена',
  'common.save': 'Сохранить',
  'test.greeting': 'Привет, {{name}}',
  'test.files': {
    one: '{{count}} файл',
    few: '{{count}} файла',
    many: '{{count}} файлов',
    other: '{{count}} файла',
  },
} as const

const dicts = { en, ru } as const
```

(Type cast мог потребовать `as unknown as Dicts` — engineer адаптирует.)

- [ ] **Step 2: Remove `test.*` keys from real dictionaries**

`en.json` — удалить `"test.greeting"` и `"test.files"`.
`ru.json` — то же.

`common.cancel` / `common.save` — оставить, они используются в реальных компонентах.

- [ ] **Step 3: Verify**

```bash
npx vitest run
```
Expected: all PASS — tests используют inline-fixtures, real dictionaries чисты.

- [ ] **Step 4: Final dictionary review**

```bash
npm run lint
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
npx vitest run
```

Все PASS. Запустить app в обоих языках, кликнуть по всем экранам — нет английских строк в RU и наоборот.

Если что-то всплыло как непереведённое — добавить ключ, обновить словари, повторить.

- [ ] **Step 5: Commit**

```bash
git add tests/renderer/i18n.test.ts src/renderer/i18n/locales/en.json src/renderer/i18n/locales/ru.json
git commit -m "chore(i18n): inline test fixtures, drop test.* from real dictionaries"
```

---

### Task 21: Final verification + manual QA

**Files:** none (verification only)

- [ ] **Step 1: Full test run**

```bash
npx vitest run
```
Expected: ALL PASS.

- [ ] **Step 2: Typecheck both projects**

```bash
npx tsc --noEmit -p tsconfig.json
npx tsc --noEmit -p tsconfig.node.json
```
Expected: PASS.

- [ ] **Step 3: Lint**

```bash
npm run lint
```
Expected: PASS (zero errors).

- [ ] **Step 4: Hardcoded EN strings sweep**

```bash
grep -RnE '>[A-Z][a-zA-Z][a-zA-Z ,.!?:;()'\''-]+<' src/renderer/components src/renderer/App.tsx
```

(Эта regex ловит JSX-content `>SomeText<`. Engineer проходит по выводу и проверяет — каждый match либо уже использует `{t(...)}`, либо это прямой английский литерал, который надо мигрировать. Иконки/символы (`⚙`, `▾`, `▸`) и числа допустимы.)

- [ ] **Step 5: Manual QA — full coverage в RU**

`npm run dev`. Через Settings переключить локаль на «Русский». Пройтись по всем экранам:
- Header (settings button title, github auth state)
- Sync tab (Pull / Push кнопки, "no repo" banner, log toggle)
- Plugins tab (catalog, presets, install/remove buttons, env prompts)
- InitWizard (4 шага)
- DeviceFlowModal
- PushModal
- Settings (все секции, error state)

Записать любой английский текст, который остался — это пропуск.

- [ ] **Step 6: Manual QA — System auto-detect**

Settings → Language → System. Проверить что отображается язык, соответствующий `app.getLocale()`. На Windows можно временно сменить язык в Region Settings (или прямо в Electron app для теста).

- [ ] **Step 7: Manual QA — error sources**

- Спровоцировать validation error в Settings (вписать `relative/path` в Local repo path) → ошибка по-русски.
- Запустить Push без auth → ошибка по-русски.
- Запустить Push при отсутствии сети → "Сетевая ошибка…".

- [ ] **Step 8: Build smoke**

```bash
npm run build
```
Expected: PASS.

- [ ] **Step 9: No commit needed for verification**

Если QA выявил пропуски — фиксы коммитятся как `fix(i18n): ...`. Если всё ок — план завершён.

---

## Done criteria

- ✅ Все 21 задач закрыты, каждая с green tests/typecheck/lint.
- ✅ В renderer нет хардкоженного английского текста (исключая иконки, цифры, имена файлов, log output).
- ✅ Main process не знает о локали; шлёт `LocalizedMessage`.
- ✅ Settings → Language работает (System / English / Русский), live switch без перезагрузки.
- ✅ `dictionaries.test.ts` — parity, plurals, placeholders OK.
- ✅ ru.json — все плюральные категории заполнены (`one`/`few`/`many`/`other`).
- ✅ Tests: `npx vitest run` — все зелёные.
- ✅ `npm run build` — без ошибок.

## Next plan

После завершения этого плана и manual QA — переходим к Sprint 2: `2026-05-08-v0.5-conflict-resolver.md` (отдельный план), где `ConflictModal`, `ThreeWayDiff` и изменения `push.ts` сразу пишутся i18n-native, используя инфраструктуру из этого спринта.
